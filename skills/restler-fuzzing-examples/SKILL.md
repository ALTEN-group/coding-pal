---
name: restler-fuzzing-examples
description: 'Reference and adapt reusable RESTler API fuzzing patterns. Use when scaffolding spec-driven grammar compilation, Dockerized RESTler execution, token-refresh authentication, or CI fuzzing gates.'
license: MIT
---

# RESTler Fuzzing Examples

On-demand scaffolding examples for the `restler-fuzzing-tests` instruction. Normative rules stay in the instruction, while task discovery and validation stay with the `RESTler Fuzz Tester` agent.

## Path Resolution

Resolve `references/` relative to this skill's install directory, the folder containing this `SKILL.md`.

## Use

Read `references/implementation-patterns.md` before scaffolding. It contains non-normative file shapes with placeholders; apply the installed instruction and replace placeholders using evidence gathered by the active agent.
