---
name: k6-performance-examples
description: 'Reference and adapt reusable k6 performance-test patterns. Use when scaffolding API load scenarios, Dockerized k6 execution, HTML and JSON reports, or CI benchmark tracking.'
license: MIT
---

# K6 Performance Examples

On-demand scaffolding examples for the `k6-performance-tests` instruction. Normative rules stay in the instruction, while task discovery and validation stay with the `Performance Tester` agent.

## Path Resolution

Resolve `references/` relative to this skill's install directory, the folder containing this `SKILL.md`.

## Use

Read `references/implementation-patterns.md` before scaffolding. It contains non-normative file shapes with placeholders; apply the installed instruction and replace placeholders using evidence gathered by the active agent.
